package server

const (
	// The local port to serve requests from
	SERVER_PORT = "8080"
	// Time in seconds to read request payload
	READ_TIMEOUT = 10
	// Time in seconds to write response payload
	WRITE_TIMEOUT = 10

	STATIC_FOLDER = "static"
	HTML_FOLDER   = "html"
)
